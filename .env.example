# Career Buddy - Environment Variables Example
# Copy this file to .env and update the values as needed
# cp .env.example .env

# ============================================
# Database Configuration (Docker)
# ============================================
# These variables are used by docker-compose.yml to configure the PostgreSQL container
# They also need to match the DATABASE_URL below for the application to connect

POSTGRES_DB=career_buddy
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# ============================================
# Database Connection String (Prisma)
# ============================================
# This is used by Prisma ORM to connect to the database
# It should match the POSTGRES_* variables above
# Format: postgresql://[user]:[password]@[host]:[port]/[database]

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/career_buddy

# For Supabase (production):
# DATABASE_URL=postgresql://<USER>:<PASSWORD>@db.<PROJECT-REF>.supabase.co:5432/postgres?sslmode=require

# ============================================
# NextAuth Configuration (Required)
# ============================================

NEXTAUTH_URL=http://localhost:3000
# Generate a secret: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-here-generate-with-openssl-rand-base64-32

# ============================================
# Google OAuth (Optional)
# ============================================
# Get these from Google Cloud Console: https://console.cloud.google.com/
# Redirect URI: http://localhost:3000/api/auth/callback/google
# Add your email under OAuth consent screen → Test users

GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ============================================
# OpenAI Configuration (Optional)
# ============================================
# If not provided, AI suggestions will be disabled or use mock mode

OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL=gpt-4o-mini
OPENAI_TIMEOUT_MS=10000

# ============================================
# AI Features Configuration
# ============================================
# Set to true to use mock AI suggestions (no API calls needed for development)
MOCK_AI_SUGGESTIONS=true
# Set to false to disable AI suggestions entirely
ENABLE_AI_SUGGESTIONS=true

# ============================================
# pgAdmin Configuration (Optional)
# ============================================
# These are used by docker-compose.yml for the pgAdmin service
# Defaults are provided in docker-compose.yml if not set

PGADMIN_DEFAULT_EMAIL=admin@career-buddy.local
PGADMIN_DEFAULT_PASSWORD=admin
